# Security Audits

No independent security audit or penetration test has been performed and
recorded for this repository. See [../README.md](../README.md) for the
report template and what exists instead today (CI's automated `npm audit`
gate — see [../../security/dependency-security.md](../../security/dependency-security.md) —
and the self-assessed threat model at
[../../security/threat-model.md](../../security/threat-model.md)).

When a real security audit is performed, add its report here as
`YYYY-MM-DD-<scope>.md` using the template in [../README.md](../README.md),
and link it from this file.
